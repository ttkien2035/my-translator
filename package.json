{
  "name": "my-translator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "node scripts/tauri-with-env.mjs dev",
    "dev:default": "tauri dev",
    "build": "tauri build",
    "lint": "cd src-tauri && cargo clippy",
    "test:js": "node --test 'src/js/**/*.test.mjs'",
    "format": "cd src-tauri && cargo fmt",
    "build:local": "node scripts/tauri-with-env.mjs build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.0"
  }
}
